# API Configuration - Change to your VPS IP when deploying
# For local development: http://localhost:5001
# For VPS deployment: http://YOUR_VPS_IP:5001
VITE_API_URL=http://localhost:5001

# Price streaming is handled entirely by the backend via MetaApi WebSocket.
# Frontend consumes prices through Socket.IO (priceStream event) and REST endpoints.
